name: Build and Deploy to Docker Hub

# 트리거 설정: main 브랜치에 push할 때 실행
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# 작업 정의
jobs:
  build-and-push:
    name: Build Docker Image and Push to Docker Hub
    runs-on: ubuntu-latest
    
    steps:
      # 1단계: 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v3
      
      # 2단계: Java 11 설정
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
      
      # 3단계: Maven 빌드
      - name: Build with Maven
        run: mvn clean package -DskipTests
      
      # 4단계: JAR 파일 확인
      - name: Verify JAR file
        run: |
          if [ ! -f target/lotto-web-1.0.0.jar ]; then
            echo "❌ JAR 파일이 생성되지 않았습니다!"
            exit 1
          else
            echo "✅ JAR 파일 생성 성공!"
            ls -lh target/lotto-web-1.0.0.jar
          fi
      
      # 5단계: Docker Hub 로그인
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      # 6단계: Docker 이미지 빌드 및 태그 지정
      - name: Build and tag Docker image
        run: |
          docker build -t jongseungpark/lotto-web:latest .
          docker tag jongseungpark/lotto-web:latest jongseungpark/lotto-web:${{ github.sha }}
          echo "✅ Docker 이미지 빌드 성공!"
      
      # 7단계: Docker Hub에 푸시
      - name: Push to Docker Hub
        run: |
          docker push jongseungpark/lotto-web:latest
          docker push jongseungpark/lotto-web:${{ github.sha }}
          echo "✅ Docker Hub 푸시 성공!"
      
      # 8단계: 완료 메시지
      - name: Deployment Summary
        run: |
          echo "🎉 배포 완료!"
          echo "이미지: jongseungpark/lotto-web:latest"
          echo "커밋 SHA: ${{ github.sha }}"
          echo "이미지: jongseungpark/lotto-web:${{ github.sha }}"

