# 데이터베이스 연결 설정
spring.datasource.url=jdbc:mariadb://localhost:3306/lotto?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=YOUR_DB_PASSWORD
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
# validate: 스키마만 검증 (기본 테이블 구조 확인)
# none: 아무것도 하지 않음
# update: 스키마 업데이트 (개발 시 사용 가능) - 테이블 자동 생성/수정
# create: 테이블 생성 (주의: 데이터 삭제됨)
# create-drop: 시작 시 생성, 종료 시 삭제

spring.jpa.show-sql=true
# SQL 쿼리를 콘솔에 출력 (개발 시 사용)

spring.jpa.properties.hibernate.format_sql=true
# SQL 쿼리를 보기 좋게 포맷팅

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
# MariaDB 방언 설정

# JPA 로깅 설정 (선택사항)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# HTTP 인코딩 설정 (Spring Boot 자동 설정 사용)
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# 로또 애플리케이션 URL 설정 (이메일 인증 링크에 사용)
app.url=http://localhost:8080

# 이메일 설정 (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=YOUR_EMAIL@gmail.com
spring.mail.password=YOUR_GMAIL_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# 이메일 인코딩 설정 (UTF-8)
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.mime.charset=UTF-8

